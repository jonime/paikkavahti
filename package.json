{
  "devDependencies": {
    "@types/cheerio": "^0.22.22",
    "@types/node-fetch": "^2.5.7",
    "prettier": "^2.1.2"
  },
  "scripts": {
    "run-check-action": "node .github/actions/check/dist/index.js",
    "build-check-action": "cd .github/actions/check && npm run build",
    "build": "npm run build-check-action"
  }
}
